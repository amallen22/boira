// const Product = require('../../../models/Product')

// function addProduct (req, res) {
//   // const title = req.body.task
//   const { url, title, subtitle, category { slug, description }, description, price } = req.body

//   const product = new Product({ url, title, subtitle, category { slug, description }, description, price })

//   product.save()
//     .then(() => res.redirect(`/products`))
// }

// module.exports = addProduct
